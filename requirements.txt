# Core dependencies. PyTorch should be installed first separately,
# pinned to the appropriate CUDA / CPU build for your system.
# See README.md for installation instructions.

torch>=2.0
torch_geometric>=2.4
numpy>=1.21
scipy>=1.7
tqdm>=4.60
pyyaml>=6.0

# Optional genomics dependencies (uncomment if needed)
# pybigwig>=0.3.18
# pyfaidx>=0.7
# cooler>=0.9
# pandas>=1.4

# Optional visualization dependencies (uncomment if needed)
# matplotlib>=3.5
# seaborn>=0.12
